# Copy to `.env.local` and adjust. Only NEXT_PUBLIC_* vars are exposed to the
# browser. No secrets are required at the bootstrap milestone (there is no
# backend — see docs/engineering/ARCHITECTURE.md §3).

# Absolute site origin, used for canonical URLs and Open Graph metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
